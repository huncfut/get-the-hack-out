/*
enemy: {
  uuid: Uuidv4,
  direction: Char(N, S, W, E),
  x: Number,
  y: Number
}
*/

// const getNextMove = (enemy, level) => return

// const generateNewEnemies = (level /*, count */) => {
//   // Filter out only the nodes
//   const nodes = level.filter(block => block.type == 'n');
//   return Array.from(Array(1/*count*/), () => )
// }
