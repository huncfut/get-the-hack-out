/*
game = {
  hacker: {
    ws: WS
  },
  player: {
    ws: WS,
    x: Number,
    y: Number,
    speed: Number // blocks / s
  },
  levels: [{
    grid: {
      height: Number,
      width: Number
    },
    nodes: [{
      x: Number,
      y: Number,
      connections: { // Indexes of other nodes
        N: Number,
        E: Number,
        S: Number,
        W: Number
      }
    }],
    guards: [{
      x: Number,
      y: Number,
      speed: Number // blocks / s
    }]
  }]
}
*/
